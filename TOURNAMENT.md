# 养蛊流程

每条算法路线只保留一个当前候选，旧实现由 Git 历史保存。

## 晋级顺序

1. 使用 `probe` 通过固定的必胜、必防、长连与四四回归。
2. 本地循环赛，每对算法在相同开局下交换黑白。
3. 出现 TLE、非法输出或禁手的版本优先淘汰，不用偶然胜局抵消。
4. 剩余版本按胜场、直接交锋、P95 墙钟耗时排序。
5. 前两名增加开局数量进行决赛。
6. 本地冠军通过 `uid:` 接口挑战线上强手，稳定后才替换根目录 `src.cpp`。

## 2026-09-04 当前结果

所有候选先通过空盘、白方一步胜和黑方四四禁手回归。四强小组赛每组对阵 4 局并
交换颜色：

| 候选 | 胜-负 | TLE | 非法 | P95 墙钟 |
| --- | ---: | ---: | ---: | ---: |
| `wallclock-fast` | 10-2 | 0 | 0 | 337.189 ms |
| 根目录 `src.cpp` | 10-2 | 2 | 0 | 937.494 ms |
| `proof-number` | 4-8 | 0 | 0 | 180.217 ms |
| `beam-search` | 0-12 | 0 | 0 | 171.574 ms |

前两名使用四组开局各交换一次颜色，决赛总比分 4:4。原版赢下全部正常结束的 4 局，
但另 4 局全部 TLE；`wallclock-fast` 没有 TLE，因此按晋级规则成为当前可靠性冠军，但
尚未替换根目录版本。

对线上 `uid:2025201726` 的无并发复核中，两版均为 1:3。`wallclock-fast` 自身没有
TLE 或非法着法；原版出现一次四四禁手。当前结论是：硬墙钟路线解决了可靠性，但棋力
仍未超过原版，下一轮应融合原版搜索质量与硬截止，而不是直接宣称已有最终冠军。

原始逐手数据在 `reports/`。`online-wallclock-vs-2025201726.json` 是按用户要求与本地
决赛并行执行的压力样本，会受到 CPU 竞争影响；干净复核使用文件名带 `-clean` 的报告。

## 当前方向

- `src.cpp`：原始 Rapfi 2018 单文件基线。
- `chatgpt-latest`：ChatGPT 网页资料库最新候选。
- `wallclock-fast`：从进程启动开始的硬墙钟与快速初始化。
- `threat-solver`：轻量威胁搜索。
- `mcts-hybrid`：固定种子的 Monte-Carlo 根层抽样。
- `beam-search`：有限宽度的确定性多层搜索。
- `proof-number`：AND-OR/证明数式强制威胁求解。

## 公平性

空棋盘的确定性对局很容易退化成“谁执黑谁赢”。正式排序必须使用多组固定开局，并对
每组开局交换算法所执颜色。所有运行从创建进程开始计墙钟，输入只有当前静态棋盘，规则
严格复刻网页 JavaScript。

为了测吞吐可以并行跑线上和本地对局，但正式比较 1 秒时限时应串行运行本地候选，避免
多个引擎争抢 CPU 造成假 TLE。
