# 养蛊流程

每条算法路线只保留一个当前候选，旧实现由 Git 历史保存。

## 晋级顺序

1. 使用 `probe` 通过固定的必胜、必防、长连与四四回归。
2. 本地循环赛，每对算法在相同开局下交换黑白。
3. 出现 TLE、非法输出或禁手的版本优先淘汰，不用偶然胜局抵消。
4. 剩余版本按胜场、直接交锋、P95 墙钟耗时排序。
5. 前两名增加开局数量进行决赛。
6. 本地冠军通过 `uid:` 接口挑战线上强手，稳定后才替换根目录 `src.cpp`。

## 2026-09-04 当前结果

所有候选先通过空盘、白方一步胜和黑方四四禁手回归。四强小组赛每组对阵 4 局并
交换颜色：

| 候选 | 胜-负 | TLE | 非法 | P95 墙钟 |
| --- | ---: | ---: | ---: | ---: |
| `wallclock-fast` | 10-2 | 0 | 0 | 337.189 ms |
| 根目录 `src.cpp` | 10-2 | 2 | 0 | 937.494 ms |
| `proof-number` | 4-8 | 0 | 0 | 180.217 ms |
| `beam-search` | 0-12 | 0 | 0 | 171.574 ms |

前两名使用四组开局各交换一次颜色，决赛总比分 4:4。原版赢下全部正常结束的 4 局，
但另 4 局全部 TLE；`wallclock-fast` 没有 TLE，因此按晋级规则成为当前可靠性冠军，但
尚未替换根目录版本。

对线上 `uid:2025201726` 的无并发复核中，两版均为 1:3。`wallclock-fast` 自身没有
TLE 或非法着法；原版出现一次四四禁手。当前结论是：硬墙钟路线解决了可靠性，但棋力
仍未超过原版，下一轮应融合原版搜索质量与硬截止，而不是直接宣称已有最终冠军。

随后加入 `replay-book-1726`：它精确记录此前原版在 `center-horizontal` 开局对该 UID
赢下的完整黑方主线。复测为 1:1，其中候选执黑第 43 手以 Five 获胜；书内平均耗时约
85ms。它证明精确静态局面开局簿有效，但属于对手/开局特化，不代表未知空盘泛化棋力。

原始逐手数据在 `reports/`。`online-wallclock-vs-2025201726.json` 是按用户要求与本地
决赛并行执行的压力样本，会受到 CPU 竞争影响；干净复核使用文件名带 `-clean` 的报告。

## 2026-09-04 第二轮：时间利用率修复

`rapfi-deadline-fusion` 此前只用掉硬截止的三分之一。原因是 Rapfi 的时间管理为整局
共享时钟设计：PV 一稳定就把 `turnTime` 按 90% 缩水，下限是预算的 1/3；而 `timeForTurn()`
读的是**剩余**时间，却拿去和**已用**时间比较，等于每轮都在追一个下移的目标。

三处改动：

- 预算改为从进程创建起算的绝对墙钟目标，软目标 820ms，不再缩水；
- 加 SIGALRM 看门狗，900ms 硬触发，异步信号安全地写出已搜到的最优合法着法后 `_exit(0)`。
  搜索每证明出一个更好的根着法就发布一次，所以任何时刻被打断都有合法结果；
- 节点计时检查从每 3000 步收紧到 1000 步，正常路径不再撞看门狗。

进程真实年龄从 `/proc/self/stat` 第 22 字段和 `/proc/uptime` 求得并计入自身时钟，
所以 exec 和动态链接的开销不再是未测量的未知数（本机实测 0-10ms，受 10ms 时钟精度限制）。

12 组开局交换黑白共 24 局（`reports/round3-timefix.json`）：

| 版本 | 胜-负 | TLE | 非法 | 平均耗时 | P95 | 最大 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 修复后 | 18-6 | 0 | 0 | 377 ms | 771 ms | 904 ms |
| 修复前 | 6-18 | 0 | 0 | 231 ms | 368 ms | 550 ms |

24 局全部以 Five 正常终局。中局单步搜索时间从约 270ms 提高到 590-790ms。

开局集在 `fixtures/openings-ab.json`：12 组不同形状的开局，每组双向各一局。它们只是
测试夹具，两个引擎在同一开局下都执一次黑一次白，所以定式本身是否符合本规则不影响公平性。

## 2026-09-04 第三轮：初始化开销与迭代停止规则

每手重启进程意味着固定开销要付 15 次以上，而不是整局一次。实测 `AI` 构造函数占
153ms，其中置换表分配 18ms，`Evaluator::init()` 建棋型表 144ms —— 每手预算的 19%，
一步棋都还没想。

棋型表那 144ms 是一段 O(n^2) 去重：65536 项两两比较约 2.5 亿次。但被保留的"规范码"
恰好就是数组的不动点（把四个数字升序放进高位得到该等价类的最小下标），一遍扫描即可
编号。改完初始化 144ms → 6ms，`PCODE`、`PATTERN4`、`PATTERN` 三张表的 FNV 校验和
与原版逐位相同（`4505005474714836504`）。

24 局对照（`reports/round4-fastinit.json`）结果是 12:12。本机打平不意外：56 个真实
局面里，把搜索时间从 256ms 提到 329ms 只改变 1/56 的着法，多数局面搜索已经收敛。
保留它的理由是评测机为 aarch64、大概率慢 2-3 倍，那 144ms 会膨胀到接近半个预算；
本机测不出收益不等于评测机上没有。这一条记为可靠性改进，不记为强度收益。

迭代停止规则是另一处为整局共享时钟写的启发式。原条件里
`timeLeft() / MATCH_SPARE_MAX < info.timeout_turn` 在按手计时下恒为真（左边最大 20，
右边是 820），整个条件塌缩成"只要某一层迭代耗时 ≥173ms 就停"，哪怕才用掉 200ms。
改为预测式：已用时间加上 1.5 倍上一层耗时若仍在预算内就继续下一层，超了就停，中途
被硬截止打断也保留已证明的结果。

24 局对照（`reports/round5-iteration.json`）：

| 规则 | 胜-负-平 | 平均耗时 | P95 | 最大 |
| --- | ---: | ---: | ---: | ---: |
| 预测式 | 17-6-1 | 354 ms | 816 ms | 905 ms |
| 原规则 | 6-17-1 | 292 ms | 735 ms | 904 ms |

## 2026-09-04 第四轮：Killer Moves 被否，开局库入库

### Killer Moves：两次实测都不值得

Rapfi 2018 在这里既没有 killer moves 也没有 history 启发式，着法排序只有置换表着法
加静态棋型分。补 killer 是个便宜的方向，实测否掉了。

先量了 `genMoves` 的分值尺度：min 20 / mean 29 / max 约 51。常见的 +1000 写法在这个
尺度下会把 killer 顶到活四活三前面，所以第一版取 +24（约一个标准差）。

| 加成 | 对主线 24 局 | 结论 |
| --- | ---: | --- |
| 24 | 8-16 | 明显有害（`reports/round6-killer.json`） |
| 8 | 13-11 | 无可测收益（`reports/round6-killer-bonus8.json`） |

两个数据点说明加成大小确实是 +24 变差的原因，但调到无害量级后也换不来收益。五子棋的
静态棋型分本身就是很强的排序信号，不像棋类里安静着法彼此难分优劣；兄弟节点的 killer
在这里相关性低。该方向记为已验证的死路，不再重试。

### 开局库

`book_build.py` 用 15 秒预算离线跑同一引擎，只记录长考与比赛预算下不一致的局面。
黑方 210 节点存 44 条，白方 412 节点存 86 条，合计 130 条已编入 `src.cpp`。

评测要用空盘：比赛就是从空盘开始，而 12 组强制开局大多落在书外。

| 版本 | 空盘对三个共同对手 | 12 组开局 24 局 |
| --- | ---: | ---: |
| 有书 | 6-0 | 12-12 |
| 无书 | 5-1 | — |

证据偏弱（每个对手只有确定性的 2 局），但方向为正且无退化，运行时代价是一次亚毫秒的
二分查找，因此入库。原始数据在 `reports/round7-book.json`。

### 并行生成

`book_build.py --jobs N` 先在父进程把树顶广度展开成若干子树（顶部节点照常搜索入库，
不会因分片丢条目），再分发给独立 worker。每个 worker 自带引擎进程和自己的置换集，
所以分片之间会重复搜到同一局面：白方 `stored=125` 而唯一条目只有 86。用重复搜索换
worker 之间零同步，因为每次查询都是墙钟搜索，等锁就是把时间片让给别人。

## 2026-09-04 第五轮：禁手判定与网页裁判不一致（会直接判负）

`contestForbiddenBlack` 里有一行短路：

```cpp
for (int d = 0; d < 4; ++d)
    if (run(move, d) == 5) return false; // exact five has priority over 4-4
```

网页裁判是反过来的。`arena_server.play_game` 和 `gomoku_match` 都先调 `black_forbidden`
再调 `winner`，`SITE_API.md` 也写明"网页即使该点同时形成恰好五连，也仍会先判四四禁手"。
所以黑棋走一个既成五连、又同时形成两个冲四的点，是**立即判负**，不是取胜。

危险的地方在于这不是偶发失误：引擎把该点算成必胜，会**主动去走**。

构造验证（`fixtures/black-five-is-four-four.txt`，该局面里禁手点是唯一的成五点）：

| 版本 | 着法 | 裁判判定 |
| --- | --- | --- |
| 修复前 | (7, 7) | FourFourBan，判负 |
| 修复后 | (3, 3) | 合法 |

先做了差分对拍：3000 个随机局面 / 61 万空点全部一致，说明随机采样打不到这个组合，
必须手工构造五连加双四的局面才能暴露。修复后重新对拍 4000 局面 / 81 万空点，仍然
零不一致，说明去掉短路没有引入新的偏差——四子计数本来就要求窗口内恰好 4 子且补空后
恰好成五，成五的那个方向自然不会被算成冲四。

`test_rules.py` 新增两条回归：一条纯规则断言，一条实际跑引擎断言它不会走出禁手点。
后者已用回退版源码验证过确实会失败。

## 2026-09-04 第六轮：Killer Moves 定论，长连误判为胜

### Killer Moves 的最终结论

第四轮用 24 局测出 bonus=8 是 13:11，比平局多一场。样本量翻倍到 58 局（29 组开局
双向，`fixtures/openings-wide.json`）后重测三个加成值：

| 加成 | 58 局 | 胜率 | 报告 |
| ---: | ---: | ---: | --- |
| 4 | 29-28-1 | 51% | `reports/round8-killer-sweep-4.json` |
| 8 | 28-28-2 | 50% | `reports/round8-killer-sweep-8.json` |
| 16 | 30-28-0 | 52% | `reports/round8-killer-sweep-16.json` |

24 局的 13:11 是噪声。三个加成全部落在 50-52%，该方向确认无收益，不再重试。
经验教训：24 局分辨不出一场胜负的差异，判断"有没有效果"至少要 58 局这个量级。

### 长连误判为胜

`Board::check5InLine` 与棋型表都把"五连及以上"算作五连，但规则是长连不计五连。
黑棋不受影响——走成长连就是长连禁手，`contestForbiddenBlack` 已标 FORBID。白棋则
可以合法走成长连，只是不算胜。

构造 `.XOOOO.OOX.`（白方补中间空点成七连，两端被黑封死，白方没有真五连点）：

| 版本 | 白方着法 | 实际结果 |
| --- | --- | --- |
| 修复前 | (7, 7) | 长连，winner() = -1，白方以为赢了 |
| 修复后 | (12, 4) | 正常着法 |

危害小于四四禁手那条（不会立即判负），但引擎会返回"必胜"并停止寻找真正取胜的着法。

修复方式与禁手一致，在增量更新处定点修正，不动 `shortLinePattern`：那是 3876 项权重
的语义核心。白棋某点若被判为 A_FIVE 但任何方向都凑不出恰好五连，降级为无威胁。

验证：400 个随机局面中筛出 229 个存在立即取胜点的，引擎全部取到了真正的胜着，
漏失 0 个，说明降级没有误伤正常的五连判定。回归局面存为
`fixtures/white-overline-not-a-win.txt`，`test_rules.py` 增加对应断言。

### 禁手修复的对局回归

`reports/round9-forbidden-fix.json`，58 局：修复后 30 - 修复前 27，平 1。日常对局
中性，符合预期——五连叠双四的组合本来就罕见。修复不花棋力，只堵掉判负的洞。

## 2026-09-04 第七轮：棋力对机器速度的敏感度

评测机是 aarch64 GCC 11.2，本机是 x86-64 GCC 14，快多少一直是个无法直接验证的盲区。
用缩减预算模拟慢机器可以量出这条曲线（`reports/round11-budget-*.json`，各 58 局）：

| 对比 | 比分 | 全预算胜率 | 折算 Elo |
| --- | ---: | ---: | ---: |
| 820ms vs 410ms（慢 2 倍） | 33-24 | 58% | 约 +56 |
| 820ms vs 273ms（慢 3 倍） | 39-19 | 67% | 约 +123 |

两个结论：

- 评测机即使慢 3 倍，也只值约 120 Elo。线上连败的主因不是机器速度，是对手更强。
- 纯速度优化的天花板很低。bitboard 重写增量评估层乐观估计 2 倍加速，只值约 56 Elo，
  代价是推翻仓库里唯一稳定的部分。该方向确认不做。

长连修复的对局回归为 29-28-1（`reports/round10-overline-fix.json`），中性，符合预期。

## 复盘工具

`analyze_game.py` 接收一局的落子序列，让引擎逐手重搜我方局面并打印根节点估值，
把"不知道怎么输的"变成"第几手开始崩的"。网页 F12 控制台里有每一手喂给程序的输入和
程序的输出，直接喂给它即可：

```sh
python3 analyze_game.py game.txt --side 0
```

估值取自 `fullSearch` 的局部变量，所以分析用的是一个改过 `main` 的临时构建，
`src.cpp` 本身不受影响、仍然只输出一行着法。搜索在必败局面提前退出时
`bestMove.value` 是未初始化的哨兵值，超出胜负区间的一律显示为"必败/必胜"而不是原始数字。

自测样例（本地一局白方负）清楚显示估值在第 15 到 17 手之间从 -30 跌到必败，
崩盘点因此可以定位到具体一手，而不是靠肉眼看棋。

## 2026-09-04 第八轮：吞吐 9 倍，线上从 0-4 变成 1-1

### 比赛适配自己成了瓶颈

`contestForbiddenBlack` 在增量更新里对每个被触及的空格无条件调用，一次落子约 32 格，
每格扫 4 方向 × 5 窗口。这是适配代码，原版 Rapfi 没有。两处修改，语义不变：

- 增量更新加必要条件前置：两种禁手都要么需要两个方向各有冲四，要么是五连及以上，
  这些在 Rapfi 自己的分类里都落在 `B_FLEX4` 或 `A_FIVE`，更弱的棋型不可能是禁手。
- 着法生成改读缓存：`pattern4[Black] == FORBID` 已经由增量更新维护且始终最新，
  生成着法时不必重算。`turnMove` 与根节点仍走直接判定，每手一次，等于给缓存加了一道
  独立复核，最终输出永远经过原始规则检查。

| 版本 | 吞吐 | 同预算平均深度 |
| --- | ---: | ---: |
| 原始 | 0.034M nodes/s | 4.1 |
| 加前置过滤 | 0.15M nodes/s | 4.9 |
| 再改读缓存 | 0.31M nodes/s | 5.2 |

合计 9.2 倍。正确性验证：缓存标记与网页裁判在 5001 个局面 / 100 万空点上零不一致
（其中真禁手 7135 个）；500 个黑方随机局面（309 个盘面存在禁手点）中引擎输出禁手 0 次。
前置过滤单独对照 58 局为 31-26-1。

### 线上对局

线上恢复访问后对 `uid:2025201726` 实测。先是 0-4 全败，两个颜色都输。逐手用时暴露了
差距的形状：对方开局 4ms 直出（有开局库），中局稳定用满 915ms。

复盘（`analyze_game.py`）显示不是某一手崩盘，而是估值持续下滑：执黑那局第 4 手 +76、
第 8 手 +60、第 12 手 +46、第 16 手 +19，第 18 手判负。

关键诊断是把预算放宽到 15 秒再打一遍：**执黑赢了**，执白仍输。这说明棋力差距在执黑一侧
是可以用深度补回来的，于是把那盘胜局的我方着法做成开局库（`book_build.py line`），
在 1 秒预算下复现：

| 版本 | 对 1726 |
| --- | --- |
| 无对手特化开局库 | 0-4 |
| 有（154 条） | 1-1，执黑 43 手成五 |

复现的那局只有 43 手而深搜原局是 49 手，说明对方中途偏离，我们仍然赢下——书给出的是
真实局面的深度着法，不是背棋谱。

`uid:2025201770` 当前提交输出 `helloworld`，四局全部因非法输出判负，暂时不是有效对手。

### 工具

- `analyze_game.py`：逐手重搜并打印根节点估值，把"不知道怎么输的"变成"第几手崩的"。
- `refute.py`：对着真实线上对手复盘，可在指定手数用 `GOMOKU_EXCLUDE` 强制走第 k 选择，
  用来搜索不输的变化线。

## 当前方向

- `src.cpp`：原始 Rapfi 2018 单文件基线。
- `chatgpt-latest`：ChatGPT 网页资料库最新候选。
- `wallclock-fast`：从进程启动开始的硬墙钟与快速初始化。
- `threat-solver`：轻量威胁搜索。
- `mcts-hybrid`：固定种子的 Monte-Carlo 根层抽样。
- `beam-search`：有限宽度的确定性多层搜索。
- `proof-number`：AND-OR/证明数式强制威胁求解。
- `rapfi-deadline-fusion`、`rapfi-aggressive`：不同硬截止/棋力边界实验。
- `rapfi-adaptive-time`：固定总预算下的 10ms 局面分类 dispatcher 原型。
- `rapfi-book-hybrid`：figrid-board Standard 开局库 + Rapfi fallback。
- `replay-book-1726`：已验证的 1726 对手/center-horizontal 精确主线。

## 公平性

空棋盘的确定性对局很容易退化成“谁执黑谁赢”。正式排序必须使用多组固定开局，并对
每组开局交换算法所执颜色。所有运行从创建进程开始计墙钟，输入只有当前静态棋盘，规则
严格复刻网页 JavaScript。

为了测吞吐可以并行跑线上和本地对局，但正式比较 1 秒时限时应串行运行本地候选，避免
多个引擎争抢 CPU 造成假 TLE。

时间管理目标是每步把安全上限用满。评测每一手重启进程、单独计 1 秒，没用完的时间
不能结转到下一手，所以"前面省时间、后面长考"是无效策略。硬截止由 SIGALRM 看门狗
兜底，正常路径不应触发它。不再追加独立的局面分类 dispatcher：Rapfi 的迭代加深本身
已经按 PV 稳定性调节预算，外挂一层会和它打架。
